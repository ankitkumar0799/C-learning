#include <stdio.h>
int main(){

    // what is preprocessor
    // c preprocessor is just a tool and it instruct the compiler to fo require prepocissing before actual compliation.

    // preprocessor directive
    // #define - defining with alias
    // #undef - undefining  the #define
    // #include - inserts a perticular header frpm another file.


    // what is header file
    // a header file is a file with extension .h which is contains c funtions declaration and macro definations
    // it is impoerted by usinf #include
    // we ca also create our own header file
    return 0;
}