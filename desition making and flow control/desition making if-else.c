#include <stdio.h>
int main(){
    int a = 5,b = 4;
    if (a < b)
    {
        printf("the %d is greater then %d \n",a,b);
    }else{
        printf("the %d is less then %d",b,a);
        
    }
    return 0;
    
    
}