#include <stdio.h>
int main(){

//     // NULL POINTER
//     int *ptr = NULL;
//     printf("\n the value of the null pointer is %d",ptr);

// // VOID POINTER
//     void *p = NULL;
//     printf("\n the size of a void pointer is %d",sizeof(p));

    // WILD POINTER
     int *w;
    printf("\n the size of a void pointer is %d",*w);
    return 0;
}